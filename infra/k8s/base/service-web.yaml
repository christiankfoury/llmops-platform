apiVersion: v1
kind: Service
metadata:
  name: ai-platform-web
  namespace: ai-platform
  labels:
    app.kubernetes.io/name: ai-platform-web
    app.kubernetes.io/component: web
    app.kubernetes.io/part-of: production-ai-platform
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ai-platform-web
  ports:
    - name: http
      port: 3000
      targetPort: http
